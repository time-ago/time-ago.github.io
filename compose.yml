name: timeago-docs

services:
    app:
        image: timeago-docs
        container_name: timeago-docs
        build: .
        volumes:
            - .:/var/www/html:z
        ports:
            - 3000:5173
